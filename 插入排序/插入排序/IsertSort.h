#ifndef _ISERT_SORT

#define _ISERT_SORT


#include<stdio.h>

#include <assert.h>

typedef int ElemType;
bool IsertSort(ElemType *p,int len);

bool  IsertSortExcellent(ElemType *p,int len);

#endif